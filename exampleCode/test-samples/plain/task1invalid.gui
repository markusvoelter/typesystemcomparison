/*
 *  validate clause must be boolean
 */
form PersonFormP1Inv edits PersonP {
	 text(20) -> name validate it.greeting;
	 text(20) -> firstName validate firstName;
	 checkbox -> isAdult validate 1;
	 checkbox -> likesCake validate firstName;
}